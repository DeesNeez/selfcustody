# Draft/unused media that isn't referenced by the live site.
# Keep files on disk for reference, but don't track them.
docs/assets/img/cash-vortex/*
!docs/assets/img/cash-vortex/exchanges-cash-vortex-final3.mp4
!docs/assets/img/cash-vortex/exchanges-cash-vortex.png
!docs/assets/img/cash-vortex/cash-vortex-core.webp
!docs/assets/img/cash-vortex/controlled-orbit.webp
!docs/assets/img/cash-vortex/controlled-orbit-v2.webp

# Not referenced today, but wanted back when the contact page opens up.
docs/assets/img/money-coins.jpeg
docs/assets/img/services-notebook.jpeg

# Superseded social preview (the @HodlDee portrait), replaced by the vault
# banner. Kept on disk for reference, but no longer tracked.
docs/assets/img/social-preview-hodldee.jpg

# Scratch space for image sources and re-exports, never served.
work/

# Node + build scratch
node_modules/
.baseline/

# Local-only preview of the real contact page (see build/preview-contact.mjs).
docs/_contact-preview.html

# macOS Finder metadata, at any level.
.DS_Store

# Local agent tooling (preview server config), not project config.
.claude/
